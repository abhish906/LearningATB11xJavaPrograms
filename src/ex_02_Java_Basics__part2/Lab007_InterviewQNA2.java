package ex_02_Java_Basics__part2;

public class Lab007_InterviewQNA2 {
    public static void main(String[] args) {
       // int enum=20;// enum is a keyword so it can't be used as an identifier
        int Enum=20;
        int a_b=40;
       // int a b=50;// not allowed because of space(identifiers can't have space)
        int a$b=50;
        int Lab013=60;
        System.out.println(Enum);
        System.out.println(a_b);
    }
}
